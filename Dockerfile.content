FROM debian:trixie-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends ca-certificates && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app

RUN mkdir -p /app/logs /content

COPY target/release/ractor-content /usr/local/bin/ractor-content

EXPOSE 8000

CMD ["ractor-content"]
