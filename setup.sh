#!/usr/bin/env bash
# Shell instructions for sandbox bootstrap live here. Replace the echo
# placeholder with the commands your sandbox needs (package installs,
# migrations, etc.). Leave the file empty if no setup is required.

echo "Update setup.sh with sandbox-specific bootstrap steps."
