// Sandbox state constants
const SANDBOX_STATE_INIT = 'init';
const SANDBOX_STATE_IDLE = 'idle';
const SANDBOX_STATE_BUSY = 'busy';
const SANDBOX_STATE_DELETED = 'deleted';


module.exports = {
  // Sandbox states
  SANDBOX_STATE_INIT,
  SANDBOX_STATE_IDLE,
  SANDBOX_STATE_BUSY,
  SANDBOX_STATE_DELETED,

  // No message roles; tasks represent full exchanges
};
